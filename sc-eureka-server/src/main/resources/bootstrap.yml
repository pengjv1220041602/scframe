server:
  port: 9000
spring:
  application:
    name: sc-euruka

# Eureka的默认注册地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
eureka:
  instance:
    instanceId: aaa